%define _bsr_utils_release %(echo %{release} | sed 's/%{?dist}$//')
Provides: /usr/sbin/weak-modules
%global __requires_exclude ^kernel\\(.*\\)
Requires: bsr-utils = %{version}-%{_bsr_utils_release}
Requires: kernel-uname-r = %{kernel_version}
