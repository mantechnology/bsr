bsr
	bsr sys-v type init script
bsr.gentoo
	gentoo specific variation of the same
bsradm.bash_completion
	bash completion for the bsradm command
bsrdisk
	resource script for heartbeat (R1 style)
block-bsr
	xen "bsr" vbd type implementation
bsr.sh.rhcs
	resource script for Red Hat Cluster Suite rgmanager
bsr.metadata.rhcs
	resource script metadata for Red Hat Cluster Suite rgmanager
outdate-peer.sh
	example implementation of the fence-peer mechanism using ssh
bsr.conf
	commented example configuration
pretty-proc-bsr.sh
	example how to pretty-print (optionally with color) /proc/bsr

build helpers:
  get_uts_release.sh
  adjust_bsr_config_h.sh 
  patch-kernel
